<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Orders</title>
    <link rel="stylesheet" href="/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>

    <div class="admin-layout">
        <aside class="sidebar">
            <div class="sidebar-header">
                <i class="fas fa-leaf logo-icon"></i>
                <span class="logo-text">FreshCart Admin</span>
            </div>
            <nav class="sidebar-nav">
                <a href="/admin" class="nav-link"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                <a href="/products" class="nav-link"><i class="fas fa-box-open"></i> Products</a>
                <a href="/orders" class="nav-link active"><i class="fas fa-shopping-cart"></i> Orders</a>
                <a href="/customers" class="nav-link"><i class="fas fa-users"></i> Customers</a>
                <a href="/analytics" class="nav-link"><i class="fas fa-chart-line"></i> Analytics</a>
                <a href="#" class="nav-link"><i class="fas fa-cog"></i> Settings</a>
            </nav>
            <div class="sidebar-footer">
                <a href="#" class="nav-link"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
        </aside>

        <main class="main-content">
            <header class="main-header">
                <h2>Order Management</h2>
            </header>

            <div class="content-wrapper">
                <div class="table-container">
                    <table class="product-table">
                        <thead>
                            <tr>
                                <th>Order ID</th>
                                <th>Customer Email</th>
                                <th>Total Amount</th>
                                <th>Order Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% orders.forEach(order => { %>
                            <tr>
                                <td data-label="Order ID">#<%= order.id %></td>
                                <td data-label="Customer Email"><%= order.customer_email %></td>
                                <td data-label="Total Amount">₹<%= parseFloat(order.total_amount).toFixed(2) %></td>
                                <td data-label="Order Date"><%= new Date(order.order_date).toLocaleDateString() %></td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</body>
</html>